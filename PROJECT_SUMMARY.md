# 基于 MatterGen 的宽禁带半导体候选晶体结构逆向设计与筛选

## 项目概述

面向新一代功率电子、深紫外光电器件对宽禁带 / 超宽禁带半导体的迫切需求，利用微软 MatterGen 扩散生成模型开展**属性引导的晶体逆向设计**。以目标电子带隙为约束条件批量生成候选晶体，借助 CHGNet 机器学习原子间势完成高通量结构弛豫与能量评估，结合形成能与几何合理性筛查，构建了一批宽禁带半导体**候选晶体结构库**。

---

## 方法

### 技术路线

```
目标带隙 (2.5 / 3.5 / 4.0 eV)
      │
      ▼
 MatterGen 条件生成 (×64)
      │
      ▼
 CHGNet 结构弛豫 (fmax < 0.05 eV/Å)
      │
      ▼
 形成能必要性筛查
      │
      ▼
 几何合理性检查
      │
      ▼
 MP 数据库 DFT 数据对比
      │
      ▼
 候选晶体结构库
```

### 晶体生成

| 项目 | 内容 |
|------|------|
| **技术路线** | 基于 MatterGen 构建带隙条件约束晶体生成流程，实现从目标电子结构性质到候选晶体结构的逆向设计 |
| **属性约束** | 基于 `dft_band_gap` 数据集微调的适配器模块，实现带隙条件生成（Classifier-Free Guidance） |
| **采样策略** | Predictor-Corrector 扩散采样，步数 N = 1000 |

### 结构验证与筛选流程

| 步骤 | 方法 | 目的 |
|------|------|------|
| **① 结构弛豫** | CHGNet v0.3.0，FIRE 优化器（fmax < 0.05 eV/Å，采用 ASE FrechetCellFilter 联合优化晶胞与原子坐标），GPU 加速 | 消除非平衡构型 |
| **② 形成能必要性筛查** | 构建 54 种元素单质参考态，计算 $E_f = (E_{\text{compound}} - \sum_i n_i \mu_i) / \sum_i n_i$ | 排除相对于元素单质参考态不稳定的结构。参考态尽量选择已知稳定结构，部分元素采用简化近似（详见局限） |
| **③ 几何合理性校验** | 原子最小间距、配位数、密度范围检查 | 剔除严重非物理结构 |
| **④ MP 数据对比** | 与 Materials Project 数据库 DFT-PBE 形成能对比 | 评估 CHGNet 预测趋势与 DFT 结果的一致性。由于 CHGNet 与 MatterGen 均主要基于 MP 数据训练，该验证不能完全视为独立验证 |

---

## 结果

### 生成样本规模

| 生成条件 | 晶体数量 | 设计目标 |
|----------|---------|---------|
| 宽带隙无约束生成 | 16 | 开放式探索 |
| 目标带隙 2.5 eV | 16 | 中等禁带半导体 |
| 目标带隙 3.5 eV | 16 | 宽禁带半导体 |
| 目标带隙 4.0 eV | 16 | 超宽禁带半导体 |
| **合计** | **64** | — |

### 形成能必要性筛查

> 形成能（$E_f$）为负值仅表明化合物相对于单质混合更稳定，不能排除其分解为其他热力学竞争物相的可能性。本分析属于必要性筛选——形成能为正是明确不稳定的充分条件，而形成能为负则是进一步研究的前提条件，并非充分条件。

```
批次                样本数    形成能范围 (eV/atom)    平均形成能
──────────────────────────────────────────────────────────────
宽带隙无约束          16      -3.54 ~ -0.80          -2.18
带隙 2.5 eV          16      -3.69 ~ -0.92          -2.39
带隙 3.5 eV          16      -4.19 ~ -0.64          -2.10
带隙 4.0 eV          16      -4.33 ~ -0.42          -2.37
──────────────────────────────────────────────────────────────
总计                 64      -4.33 ~ -0.42          -2.26
```

> 注：形成能范围反映不同化学体系之间的组成差异，并非同一体系内的不确定性或误差范围。

- 64 个生成结构均满足负形成能条件，通过形成能必要性筛查
- 64 个候选结构均成功完成 CHGNet 弛豫，无结构优化失败样本
- 弛豫前后体积变化普遍 < 2%，说明 MatterGen 生成结构具有较好的初始几何合理性
- 几何合理性校验全部通过，未发现原子重叠、配位数异常等非物理结构

### 与 Materials Project DFT 数据对比分析

由于生成体系较新，仅部分组成在 Materials Project 数据库中已有记录。对可匹配的 19 个体系进行 CHGNet 与 DFT-PBE 形成能直接对比：

| 指标 | 数值 | 说明 |
|------|------|------|
| 匹配样本数 | 19 | 同组成在 MP 中可查；样本量有限，仅能反映可匹配体系中的趋势一致性 |
| 形成能符号一致率 | 100% | 可匹配体系中趋势一致（数据同源性问题详见研究局限） |
| 平均绝对误差 (MAE) | 0.224 eV/atom | 在 ML 势函数合理范围内 |
| 系统偏差 | -0.165 eV/atom | CHGNet 略偏负 |

### 结构新颖性分析

利用 pymatgen `StructureMatcher` 将 64 个生成结构与 Materials Project 中同组成的已知结构进行全量比对：

- **已知结构**（与 MP 同构）：**9** 个（如 ScF₃、MgSe、Nd₂P₂O₈）
- **新晶型**（同组成、不同构型）：**10** 个（如 TbF₃、Lu₂F₂O₂、Cs₂Br₂）
- **新结构**（MP 未收录的新化学计量比）：**45** 个

> 64 个结构中，19 个具有与 MP 已知相相同的化学组成（其中 9 个同构、10 个为潜在新晶型）；剩余 45 个为 MP 未收录的新组成，构成项目的核心候选库。

### 候选晶体结构库（代表性结构）

| 化学式 | 生成分组 | CHGNet Ef (eV/atom) | MP (DFT-PBE) Ef | 体系特点 |
|--------|---------|-------------------|-----------|---------|
| F₃Tb | 带隙 4.0 eV | -4.33 | -4.50 | 稀土氟化物 |
| F₃Sc | 带隙 3.5 eV | -4.19 | -4.22 | 氟化钪 |
| F₂Lu₂O₂ | 带隙 4.0 eV | -4.17 | -4.32 | 稀土氧氟化物 |
| Nd₂O₈P₂ | 带隙 2.5 eV | -3.69 | -3.29 | 稀土磷酸盐 |
| F₆HoRb₃ | 宽带隙 | -3.54 | -3.46 | 碱金属-稀土氟化物 |

> 候选库集中于稀土氟化物 / 氧氟化物体系。新颖性分析表明，TbF₃、Lu₂F₂O₂ 等属于已知组成的新晶型（同组成不同构型）；HoRb₃F₆、NbO₄Ta 等为 MP 未收录的新组成。含 f 电子稀土体系的稳定性与电子性质需高精度 DFT 进一步验证。

### F-Tb 体系 CHGNet 统一标尺凸包分析（ML 势框架下）

以最优候选 TbF₃ 为例，下载 Materials Project 中 F-Tb 体系全部已知竞争相（5 个），使用 CHGNet 统一弛豫获得一致能量标尺，构建凸包并定位生成结构。以下结果基于机器学习势框架，不代表 DFT 水平结论：

| 指标 | 数值 |
|------|------|
| F-Tb 竞争相数 | 5 |
| TbF₃ CHGNet 总能 | -27.97 eV |
| TbF₃ Ehull（ML 势框架下） | **0.039 eV/atom** |
| 分解相 | TbF₃（已知相） |
| 判定 | **亚稳态**（$E_{\text{hull}} < 0.1$ eV/atom，ML 势下） |

> 在 CHGNet 统一能量标尺下，MatterGen 生成的 F₃Tb 被判定为 **亚稳态**（$E_{\text{hull}} = 0.039$ eV/atom），说明这是一种不同于 MP 已知 TbF₃ 相（$E_{\text{hull}} = 0$）的新晶型。该结论基于机器学习势框架，仍需 DFT 交叉验证。

---

## 项目代码结构

```
batch_generate.py     # MatterGen 批量采样
validate.py           # CHGNet 弛豫 + 形成能 + 几何检查
calibrate_chgnet.py   # CHGNet 与 MP-DFT 数据对比
novelty_analysis.py   # 结构新颖性分析 (vs MP)
convex_hull.py        # F-Tb 体系 CHGNet 自建凸包
project_analyze.py    # 结果统计与分析
```

---

## 技术栈

| 类别 | 工具 |
|------|------|
| 生成模型 | MatterGen（扩散模型 + Adapter 微调） |
| 原子势计算 | CHGNet v0.3.0（图神经网络原子间势） |
| 模拟框架 | ASE / pymatgen |
| DFT 基准 | Materials Project (PBE) |
| 深度学习框架 | PyTorch |

---

## 研究局限与后续方向

1. **元素参考态存在近似**：为计算形成能构建了 54 种元素参考态数据库，部分复杂元素参考态采用简化模型，可能引入一定系统误差。后续可基于 ICSD 已知结构完善。
2. **凸包覆盖范围有限**：已对 F-Tb 体系完成统一 CHGNet 标尺下的凸包分析，但尚未覆盖全部 64 个候选体系的凸包。后续可推广至 F-Sc、F-Lu-O 等其他核心体系。
3. **CHGNet 精度已初步量化，但存在同源偏差**：MAE 0.224 eV/atom，但该精度评估基于与 MP-DFT 的对比，而 CHGNet 与 MatterGen 共享同源训练数据（均主要来自 Materials Project），该一致不能完全视为独立验证。
4. **稀土体系预测精度存在额外不确定性**：含 f 电子的稀土元素电子关联效应较强，PBE 泛函及基于其训练的 ML 势函数在此类体系中的预测精度可能低于主族体系。相关候选结构需进一步采用 PBE+U 或 HSE06 等方法确认。
5. **缺少动力学稳定性验证**：尚未开展声子色散计算。
6. **带隙值需 DFT 验证**：当前为条件生成的目标值而非计算值，需 HSE06 等杂化泛函确认。

---

## 项目定位

| 维度 | 说明 |
|------|------|
| **科学问题** | 宽禁带 / 超宽禁带半导体新体系探索 |
| **技术路线** | 生成式 AI → 高通量 ML 筛选 → 候选池构建 |
| **当前阶段** | AI 生成 + ML 初步筛选完成，待 DFT 精确验证 |
| **项目性质** | 本科阶段材料信息学科研训练项目 |
| **预期价值** | 为后续精确验证和实验探索提供理论候选结构库 |
| **生成规模说明** | 64 个样本用于建立从生成、弛豫、筛选到验证的完整 pipeline，而非百万级高通量搜索 |

## 引用

- MatterGen: Zeni et al., *Nature* (2025) (MIT License)
- CHGNet: Deng et al., *Nature Machine Intelligence* (2023) (MIT License)
- Materials Project: Jain et al., *APL Materials* (2013)

---

---

## 复试版精简摘要

> **基于 MatterGen 的宽禁带半导体候选晶体结构逆向设计与筛选（2025–2026）**
>
> 面向功率电子器件对宽禁带半导体需求，开展生成式 AI 辅助晶体结构设计。基于 MatterGen 扩散生成模型，通过带隙属性约束生成 64 组候选晶体结构；利用 CHGNet 机器学习原子势完成结构弛豫、形成能计算及几何合理性筛选。系统新颖性分析显示：9 个与 MP 已知结构同构，10 个为同组成新晶型，45 个为 MP 未收录的新组成。以最优候选 TbF₃ 为例，在统一 CHGNet 能量标尺下构建 F-Tb 体系凸包，确认其为亚稳态（$E_{\text{hull}} = 0.039$ eV/atom，ML 势框架）。候选结构的带隙值与动力学稳定性需后续 DFT 计算精确确认。

---

*项目开展时间: 2025–2026*
*目标: 电子科技大学 材料物理与化学 考研复试科研素材*
